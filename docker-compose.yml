services:
  backend:
    build: ./backend
    ports:
      - "8080:8080"
    volumes:
      - ./backend/tickets.db:/app/tickets.db
    env_file:
      - .env
    restart: unless-stopped

  logger:
    build: ./logger
    volumes:
      - ./logs:/logs
    env_file:
      - .env
    restart: unless-stopped
    depends_on:
      - backend

  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    environment:
      - VITE_API_URL=http://localhost:8080
    depends_on:
      - backend
    restart: unless-stopped